[package]
name = "stuff"
version = "0.1.0"
edition = "2024"

[dependencies]
snow = { version = "0.10.0", default-features = false, features = ["use-curve25519", "use-chacha20poly1305", "use-blake2"] }
tokio = { version = "1.47.1", features = ["rt", "rt-multi-thread", "net", "time"] }
